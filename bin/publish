#!/usr/bin/env -S bash -e -o pipefail

SCRIPT_DIR=$(dirname -- "${BASH_SOURCE[0]}")
cd "$SCRIPT_DIR/.."

./bin/build
npm publish \
  --access public \
  --registry https://registry.npmjs.org/ \
  --tag latest
